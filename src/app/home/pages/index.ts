import { HomePage } from './home/home.page';
import { HomeRootPage } from 'home/home-root.page';

/**
 * Variável utilizada para importar as paginas em um só lugar
 */
export const homePages: any = [
    HomePage,
    HomeRootPage
];
